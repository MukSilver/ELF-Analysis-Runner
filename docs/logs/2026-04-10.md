# 2026-04-10

## 오늘 한 일
- analyze_file() 흐름을 최종적으로 정리했다.
- PIE → Program Header → dynamic entries → is_dynamic → interpreter → NX → RELRO → Canary → return 순서가 정상 동작하도록 수정했다.
- return 아래에 있던 코드, except 안으로 잘못 들어간 코드, 들여쓰기 문제를 계속 수정하면서 흐름을 바로잡았다.
- 현재 기준으로 arch, is_dynamic, interpreter, nx, pie, relro, canary까지 분석 결과가 나오도록 만들었다.
- 다음 단계로 JSON 저장을 붙이면 된다는 지점까지 왔다.

## 오늘 배운 것
- 분석 로직이 맞더라도 코드 위치가 틀리면 결과가 전혀 안 나올 수 있다는 걸 많이 체감했다.
- return은 함수 종료라서, 그 아래 코드는 무조건 실행되지 않는다는 점을 다시 확실히 익혔다.
- except는 로직을 대신하는 게 아니라, 정상 흐름 바깥의 예외 처리라는 점을 더 분명하게 이해했다.
- 분석기 프로젝트에서는 기능 구현만큼 흐름 검증도 중요하다는 걸 느꼈다.


## 지금까지 만든 것
- ELF 분석기 1차 핵심 완성본
- read_program_headers()
- read_dynamic_entries()
- analyze_file()
- print_analysis_result()
- 보호기법 4종 포함 분석 결과 출력 구조
- 현재 코드상 run_target()은 아직 "Run feature is not implemented yet." 상태라서 다음 단계는 실행기와 JSON 저장이다.


## 느낀 점
- 오늘은 새로 기능들을 넣기보단, 지금까지 추가한게 많아서, 제대로 돌아가게 다듬는 작업을 했다.
- 또 한번 느끼지만, 처음에 설계할 때 확실히 설계했으면, 지금처럼 추가해 나가면서 안해도 되는거라 덜 힘들었을거라 생각이 된다.
- 그래도 여기까지 오니까 프로젝트 초반은 넘겼다는 느낌이 든다.
- 구현보다 디버깅이 더 오래 걸릴 수도 있겠다는 걸 확실히 느꼈다.

## 막힌 점
- canary 코드를 except 안에 넣었다가, 다시 return 아래에 두었다가, 또 들여쓰기 때문에 note가 특정 경우에만 찍히는 등 자잘한 흐름 오류가 반복됐다.
- 어디가 “논리 오류”이고 어디가 “구조 오류”인지 구분하는 데 시간이 걸렸다.

## 해결한 점
- canary는 정상 분석 흐름 안, return 전에 둬야 한다는 걸 최종적으로 정리했다.
- except는 그대로 유지하되, 예외 발생 시 note만 남기고 반환하는 구조로 확정했다.
- 현재는 analyze 코어가 거의 끝났고, 다음은 JSON 저장 → run → restricted 순서라는 흐름을 분명히 잡았다.

## 내일 할 일
- analyze 결과 JSON 저장 기능 추가
- reports/analyze/ 폴더 자동 생성
- 저장 파일명 규칙 정하기
- 샘플 여러 개로 결과 확인하기
- 이후 run 기능 설계 시작하기